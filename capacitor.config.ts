import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'lib-khitan-humaira-angular',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
